npm install 
to install dependecies.

npm start
to run metro

and 
npm run android
to run on android

pod install (For IOS)
to install pods packages after doing npm install.

npm run ios
to run on ios 
